apiVersion: v1
kind: Service
metadata:
  name: fcg-purchase-service
  labels:
    app: fcg-purchase-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: fcg-purchase-service
